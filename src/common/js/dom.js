export function getEleIndex (el, name, val) {
  const prefix = 'data-'
  name = prefix + name
  console.log(el)
  // if (val) {
  //   return el.setAttribute(name, val)
  // } else {
  //   return el.getAttribute(name)
  // }
}
